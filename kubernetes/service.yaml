apiVersion: v1
kind: Service
metadata:
  name: sandkube-service
  namespace: sandkube-ns
  labels:
    app: sandkube-app
spec:
  type: LoadBalancer
  selector:
    app: sandkube-app
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 3000